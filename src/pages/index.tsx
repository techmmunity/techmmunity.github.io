import { HomeTitle } from "components/HomeTitle";

import { Container, HomeSection } from "styles/pages/Home";

const Home = () => (
	<Container>
		<HomeSection>
			<HomeTitle />
		</HomeSection>
	</Container>
);

export default Home;
